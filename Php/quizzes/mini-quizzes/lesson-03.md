# Mini-quiz — Lesson 3: Operators
**8 questions · 5 minutes**

Score: ____ / 8

---

**1.** Explain the difference in one sentence each:

`=`  means ______________________________________

`==` means ______________________________________

**2.** Fill the table:

| Code | Result |
|---|---|
| `7 + 3 * 2` | ______ |
| `(7 + 3) * 2` | ______ |
| `7 % 3` | ______ |
| `2 ** 3` | ______ |

**3.** `$x = 20;` then `$x -= 5;` then `$x *= 2;` — what is `$x`?

Answer: ______

**4.** True or false?

```php
5 == "5"      // ______
5 === "5"     // ______
5 != 4        // ______
```

**5.** `$i = 7;` — what does `$i++` do to it?

Answer: ______________________________________

**6.** Write the condition: the person is 18 or older **AND** has a passport
(`$age`, `$hasPassport`):

```php
_________________________________
```

**7.** Write the condition: it is Saturday **OR** it is Sunday (`$day`):

```php
_________________________________
```

**8.** Rewrite this using the ternary operator:

```php
if ($mark >= 10) {
    $result = "Pass";
} else {
    $result = "Fail";
}
```

```php
$result = _________________________________
```

---
✅ 7–8 → lesson 4 · 5–6 → redo exercise 3 (predict then run) · under 5 → the `=` vs `==` page again
